import React from 'react'
import { auth } from '../auth'
import { redirect } from 'next/navigation';

async function page() {
    return (
        <div>
            hello
        </div>
    )
}

export default page